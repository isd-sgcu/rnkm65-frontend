import { styled } from 'config/stitches.config'

export const CropperContainer = styled('div', {
  position: 'relative',
  display: 'flex',
  alignItems: 'center',
  maxWidth: '100%',
  height: '200px',
})
